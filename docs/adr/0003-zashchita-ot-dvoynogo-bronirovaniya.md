# ADR 0003. Защита от двойного бронирования одного слота

- **Статус:** принято
- **Дата:** 30 сентября 2026
- **Авторы:** команда курса «ИИ для разработчиков», СФУ

## Контекст

Главное правило предметной области: на один слот может быть не более одной
действующей брони. Если правило нарушится, два гостя придут к организатору
на одно и то же время.

Ситуация, в которой правило легко нарушить: два запроса приходят почти
одновременно. Оба проверяют, что слот свободен, оба видят, что свободен,
и оба создают бронь. Такая ошибка называется состоянием гонки. Проверка в коде
сама по себе от неё не спасает.

Дополнительные условия:

- Слоты не хранятся в базе (смотри ADR 0002), поэтому повесить ограничение
  на `slot_id` нельзя.
- Отменённая бронь не должна мешать забронировать слот заново.
- Хранилище это SQLite с синхронным драйвером (смотри ADR 0001).

## Рассмотренные варианты

**Только проверка в коде.** Просто и понятно. Отклонено: не спасает от состояния
гонки между процессами. В базе окажутся две брони на один слот, и заметят это
только на встрече.

**Только уникальный индекс, без предварительной проверки.** Тоже корректно
и короче. Отклонено по учебным соображениям: обработка исключения базы как
штатного сценария читается сложнее, чем явная проверка. Явная проверка ещё
и экономит попытку записи в подавляющем большинстве обращений.

**Транзакция уровня `SERIALIZABLE` или блокировка таблицы.** Даёт ту же
гарантию. Отклонено: тяжелее по нагрузке и требует объяснения уровней изоляции
транзакций, а это выходит за рамки курса.

**Уникальное ограничение по всем полям, включая `status`.** Не требует
частичного индекса. Отклонено: тогда нельзя было бы отменить бронь и создать
новую, а потом отменить и её. Вторая отменённая бронь на тот же слот нарушила бы
ограничение.

## Решение

Защита сделана в два рубежа.

**Первый рубеж, проверка в коде.** В `server/src/routes/bookings.ts` перед
созданием брони выполняется запрос: есть ли уже бронь на эту активность, дату
и время начала со статусом `active`. Если есть, сервис отвечает кодом 409,
кодом ошибки `slot_taken` и текстом «Этот слот уже забронирован».

**Второй рубеж, уникальный индекс в базе.** В `server/src/db.ts` объявлен
частичный уникальный индекс:

```sql
CREATE UNIQUE INDEX IF NOT EXISTS uq_active_booking
  ON bookings (activity_id, date, start_time)
  WHERE status = 'active';
```

Он не даёт записать в базу вторую действующую бронь на тот же слот, даже если
оба запроса прошли первую проверку. При нарушении индекса драйвер возбуждает
исключение с кодом, начинающимся на `SQLITE_CONSTRAINT`. Обработчик распознаёт
его функцией `isUniqueViolation` и отвечает тем же кодом 409 с тем же текстом.
Для клиента оба рубежа выглядят одинаково.

Условие `WHERE status = 'active'` означает, что отменённые брони под ограничение
не попадают. Поэтому отменённый слот можно забронировать заново, а история
отмен сохраняется.

Отдельно стоит сказать, зачем нужны оба рубежа при синхронном драйвере.
Синхронный запрос действительно не даёт другой задаче вклиниться между
проверкой и записью внутри одного процесса. Но процессов может быть несколько:
за одним файлом базы могут стоять два запущенных сервиса, а после перехода
на PostgreSQL их будет столько, сколько экземпляров развёрнуто. Гарантию должна
давать база, а не расположение строк в коде.

## Последствия

**Положительные:**

- Правило соблюдается даже при одновременных запросах из разных процессов.
  Гарантию даёт база данных, а не порядок выполнения кода.
- Сообщение об ошибке остаётся понятным. Клиент получает код `slot_taken`
  и текст на русском языке, а не служебное сообщение драйвера.
- Отмена брони освобождает слот и при этом не удаляет запись. Видно, кто
  бронировал и кто отменил.
- Получается наглядный учебный пример: тест «уникальный индекс в базе не даёт
  записать вторую действующую бронь» обходит проверку в коде и обращается
  прямо к хранилищу, то есть показывает, что индекс действительно срабатывает.

**Отрицательные:**

- Логика продублирована: правило записано и в коде, и в индексе. При изменении
  правила нужно поменять оба места.
- Частичные индексы есть не во всех базах данных. SQLite и PostgreSQL их
  поддерживают, MySQL до версии 8.0 нет.
- Индекс создаётся вместе с таблицами. Если база уже существовала до появления
  индекса, её нужно пересоздать: миграций в проекте нет.
- Распознавание нарушения по префиксу кода ошибки привязано к драйверу.
  Проверка вынесена в одну функцию `isUniqueViolation`, чтобы при смене
  хранилища менять одно место.
