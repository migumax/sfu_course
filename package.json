{
  "name": "booking-reference",
  "version": "1.0.0",
  "private": true,
  "description": "Эталонный проект курса «ИИ для разработчиков»: сервис бронирования тайм-слотов",
  "engines": {
    "node": ">=22.12 <23"
  },
  "workspaces": [
    "contract",
    "server",
    "web",
    "e2e"
  ],
  "scripts": {
    "contract:build": "npm run build --workspace contract",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "seed": "npm run seed --workspace server",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web && npm run typecheck --workspace e2e",
    "test": "npm run test --workspace server",
    "e2e": "npm run test --workspace e2e",
    "start": "npm run start --workspace server"
  }
}
